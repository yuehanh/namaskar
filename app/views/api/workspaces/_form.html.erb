<%= form_with(model: api_workspace, local: true) do |form| %>
  <% if api_workspace.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_workspace.errors.count, "error") %> prohibited this api_workspace from being saved:</h2>

      <ul>
      <% api_workspace.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :creator_id %>
    <%= form.text_field :creator_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
